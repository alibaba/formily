import React from 'react'
import styled from 'styled-components'

export const RightPanel = styled(({ className }) => {
  return <div className={className}></div>
})`
  border-left: 1px solid #b3b3b34a;
  flex-grow: 2;
`
