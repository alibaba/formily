import { createForm } from '../index'

test('createForm', () => {
  //todo
})

test('submit', () => {
  //todo
})

test('reset', () => {
  //todo
})

test('validate', () => {
  //todo
})

test('setState', () => {
  //todo
})

test('getState', () => {
  //todo
})

test('subscribe', () => {
  //todo
})

test('unsubscribe', () => {
  //todo
})

test('setFieldState', () => {
  //todo
})

test('getFieldState', () => {
  //todo
})

test('registerField', () => {
  //todo
})

test('registerVField', () => {
  //todo
})

test('createMutators', () => {
  //todo
})
