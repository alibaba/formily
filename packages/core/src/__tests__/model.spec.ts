import { createStateModel } from '../shared/model'

test('createStateModel', () => {
  //todo
})

test('proxy model', () => {
  test('subscribe', () => {
    //todo
  })
  test('unsubscribe', () => {
    //todo
  })
  test('batch', () => {
    //todo
  })
  test('getState', () => {
    //todo
  })
  test('setState', () => {
    //todo
  })
  test('getSourceState', () => {
    //todo
  })
  test('setSourceState', () => {
    //todo
  })
  test('setState', () => {
    //todo
  })
  test('hasChanged', () => {
    //todo
  })
  test('getChanged', () => {
    //todo
  })
})

test('dirty model', () => {
  test('subscribe', () => {
    //todo
  })
  test('unsubscribe', () => {
    //todo
  })
  test('batch', () => {
    //todo
  })
  test('getState', () => {
    //todo
  })
  test('setState', () => {
    //todo
  })
  test('getSourceState', () => {
    //todo
  })
  test('setSourceState', () => {
    //todo
  })
  test('setState', () => {
    //todo
  })
  test('hasChanged', () => {
    //todo
  })
  test('getChanged', () => {
    //todo
  })
})
